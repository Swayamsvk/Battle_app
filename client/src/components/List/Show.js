import React from "react";

const Show = ({ data }) => {
  return (
    <div style={{ marginLeft: "2vw", fontSize: "2vw" }}>
      <div></div>
      <p>{data.location}</p>
    </div>
  );
};

export default Show;
